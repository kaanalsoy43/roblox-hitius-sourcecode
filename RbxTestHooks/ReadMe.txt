========================================================================
    DYNAMIC LINK LIBRARY : RbxTestHooks Project Overview
========================================================================

This is an optional, delay-loaded dll.

If the .dll file is present, certain testing functions will be enabled.

At this time:

IWorkspace::ExecScript only works when this .dll file is present.